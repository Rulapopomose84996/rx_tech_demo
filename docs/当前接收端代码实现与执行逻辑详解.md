# 当前接收端代码实现与执行逻辑详解

## 1. 文档目的

本文档只描述当前仓库已经落地的接收端实现，不把历史 AF_XDP 路径、旧目录结构或未实现规划写成既成事实。

当前唯一主线位于 `src/receiver`，并以 DPDK 为入口。

**当前阶段**：Phase 5（CPI 输出流水线）已完成。

**Phase 3 → Phase 5 演进总结**：

- Phase 3 完成模块化重构：主循环 `OwnerLoop::run()` 从 1000+ 行缩减至约 155 行，按职责拆分为 PacketPipeline、CpiStateCoordinator、DataOrderTracker、CaptureSink、RuntimeStatusReporter
- Phase 5 完成 CPI 输出流水线：引入 lock-free SpscRing、原子化 CpiContextPool（4 slot CAS 池）、CpiConsumer 后台消费线程、ReleaseToken 回收机制，实现从 finalize 到消费再到池回收的完整异步闭环

## 2. 当前源码结构

### 2.1 目录总览

| 目录 | 作用 | 关键组件 |
|---|---|---|
| `include/rxtech` | 公共头入口（22 个头文件） | 全部公共接口与数据结构 |
| `src/receiver/app` | 可执行入口、CLI 解析、启动装配 | run_app、cli_args |
| `src/receiver/runtime` | 配置、运行器、停止控制、raw_record | RxConfig、ReceiveRunner、RawFrameRecorder |
| `src/receiver/ingress/dpdk` | DPDK 收包与 ARP 应答 | DpdkIngress、ArpResponder |
| `src/receiver/protocol` | UDP payload 提取、解析、校验、序列解释 | PacketPipeline、DataOrderTracker |
| `src/receiver/core` | owner loop、CPI 状态协调 | OwnerLoop、CpiStateCoordinator |
| `src/receiver/sidecar` | 指标与监控报告 | MetricsCollector、RuntimeStatusReporter、StatusPanel |
| `src/receiver/storage` | slot 写入、进度推进 | SlotWriter、ProgressTracker |
| `src/receiver/admit` | CPI 准入决策 | CpiAdmission、RecentClosedRing |
| `src/receiver/finalize` | CPI finalize 和 finalized output 结构 | CpiFinalizer、CpiOutput、ReleaseToken |
| `src/receiver/output` | CPI 消费线程、capture 落盘 | CpiConsumer、CaptureSink |

源码规模：34 个模块文件（24 .cpp + 10 内部 .h），22 个公共头文件。

### 2.2 构建目标

当前构建产物（见 `src/receiver/CMakeLists.txt`）：

| 目标 | 类型 | 说明 |
|---|---|---|
| `rx_receiver_core` | 静态库 | 聚合核心流水线（19 个源文件） |
| `rx_receiver_dpdk_ingress` | 静态库 | DPDK 后端（2 个源文件） |
| `rx_receiver_app` | 静态库 | 应用层（2 个源文件） |
| `rx_receiver_dpdk` | 可执行 | 主接收入口 |
| `rxbench_dpdk` | 可执行 | benchmark 变体 |

它们从 `app/main_dpdk.cpp` 进入统一的 `run_app("dpdk", argc, argv)`。

### 2.3 核心组件说明

#### PacketPipeline (`src/receiver/protocol/packet_pipeline.{h,cpp}`)
- **职责**：封装完整的包处理流水线，统一调度 UdpPayloadAssembler、Parser、Validator、Interpreter
- **输入**：PacketDesc（原始数据包描述）
- **输出**：ProcessedPacket（包含 udp_frame、parsed、interpreted 的完整视图）

#### DataOrderTracker (`src/receiver/protocol/data_order_tracker.{h,cpp}`)
- **职责**：监控数据包的顺序完整性，检测乱序、丢包等异常
- **功能**：记录期望的下一个 CPI/PRT/channel/packet_index，比对实际接收包，统计失配次数

#### CpiStateCoordinator (`src/receiver/core/internal/cpi_state_coordinator.h`)
- **职责**：CPI 状态管理中枢，协调 admission、context pool、writer、progress、finalizer
- **Phase 5 增强**：持有 CpiContextPool（原子化 4-slot 池），通过 SpscRing 连接输出流水线
- **功能**：
  - 处理数据包的 CPI 准入决策（委托 CpiAdmission）
  - 管理活动 CPI 上下文的 pool acquire/release
  - finalize 后推送 CpiOutput 到 output_ring
  - drain_recycle() 从 recycle_ring 回收 ReleaseToken 并释放池位

#### CaptureSink (`src/receiver/output/internal/capture_sink.h`)
- **职责**：格式化输出处理后的数据包到 capture 文件
- **功能**：写入 capture_packets.bin（二进制载荷）和 capture_index.csv（语义索引），批量刷新优化 I/O

#### RuntimeStatusReporter (`src/receiver/sidecar/internal/runtime_status_reporter.h`)
- **职责**：周期性状态报告和最终总结生成
- **功能**：按 status_interval_seconds 周期重绘中文状态面板，构建 RunSummary，渲染最终报告

#### CpiConsumer (`src/receiver/output/cpi_consumer.cpp`)
- **Phase 5 新增**：后台消费线程
- **职责**：从 output_ring 弹出 CpiOutput，调用用户 handler 回调，推送 ReleaseToken 到 recycle_ring
- **生命周期**：由 OwnerLoop 在主循环前启动、循环结束后 join

#### SpscRing (`include/rxtech/spsc_ring.h`)
- **Phase 5 新增**：lock-free 单生产者/单消费者环形缓冲区模板
- **实现**：原子操作 + 缓存行对齐（64 字节），memory_order_acquire/release 同步
- **容量语义**：min_capacity → 内部上取 power-of-two → 可用 slot = (power_of_two - 1)

#### CpiContextPool (`include/rxtech/cpi_context_pool.h`)
- **Phase 5 增强**：4-slot 上下文池，使用 `std::atomic<bool>` CAS 操作实现 lock-free acquire/release
- **关键约束**：CpiContext 体量约 16 MB，必须堆分配（栈分配会导致服务器端栈溢出）

## 3. 启动与配置流程

当前启动主流程在：

- `src/receiver/app/main_dpdk.cpp`
- `src/receiver/app/run_app.cpp`
- `src/receiver/app/cli_args.cpp`

实际顺序是：

1. 解析 CLI
2. 加载默认配置
3. 若提供 `--config`，加载 section 化配置文件
4. 强制把 `backend_name` 设为当前入口对应后端，这里是 `dpdk`
5. 校验 capture / raw_record / protocol / log 关键字段
6. `--dry-run` 时打印展开后的最终配置
7. 非 dry-run 时创建 backend、metrics，并进入 `ReceiveRunner`

当前示例配置的重点 section 为：

- `[capture]`
- `[raw_record]`
- `[network]`
- `[dpdk]`
- `[runtime]`
- `[protocol]`
- `[log]`
- `[feedback]`

## 4. ReceiveRunner 做什么

`src/receiver/runtime/receive_runner.cpp`（约 320 行）负责运行期框架动作：

- 安装 `SIGINT` / `SIGTERM` 停止处理
- 初始化 backend
- 准备 capture/raw_record/log 输出目录和文件路径
- 打开 `capture_packets.bin` 和 `capture_index.csv`
- 启动 `RawFrameRecorder`（后台写入线程，支持文件轮转）
- 调用 `OwnerLoop::run()`
- 收集 `CaptureArtifacts`
- 补齐 `RunSummary`（由 RuntimeStatusReporter 完成）
- 关闭 recorder 和 backend

当前 capture index 表头是：

```
cpi,channel,prt,packet_index,packet_kind,payload_len,valid
```

## 5. Owner Loop 的真实热路径

### 主循环架构

真正的主循环在 `src/receiver/core/owner_loop.cpp`（约 155 行）。

**主循环职责**：
- 创建和初始化所有模块化组件
- 创建 CPI 输出流水线的两个 SpscRing（output_ring + recycle_ring）
- 启动 CpiConsumer 后台线程
- 执行收包循环，委托具体业务给子组件
- 调用 RuntimeStatusReporter 进行周期性状态报告
- 循环结束后 join 消费线程、构建最终总结

### 当前热路径流程

```
OwnerLoop::run() 初始化阶段
  -> 创建 PacketPipeline、CpiStateCoordinator、DataOrderTracker、CaptureSink
  -> 创建 SpscRing<CpiOutput> output_ring (capacity=8)
  -> 创建 SpscRing<ReleaseToken> recycle_ring (capacity=8)
  -> CpiStateCoordinator.attach_rings(output_ring, recycle_ring)
  -> 启动 CpiConsumer 后台线程 (consumer_thread)
  -> 创建 RuntimeStatusReporter

收包循环:
  DpdkIngress.recv_burst
    -> for each packet:
         -> RawFrameRecorder.submit (如启用)
         -> PacketPipeline.process_packet()
              -> UdpPayloadAssembler.push (提取 UDP payload)
              -> matches_packet_filter (过滤)
              -> PacketParser.parse (解析)
              -> PacketValidator.validate (校验)
              -> ProtocolSequenceInterpreter.interpret (序列解释)
         -> on_packet 回调:
              -> control_table: CpiStateCoordinator.process_control_packet
              -> data_packet:
                   -> DataOrderTracker.observe (顺序监控)
                   -> CpiStateCoordinator.process_data_packet
                        -> CpiAdmission.judge (准入决策)
                        -> open_active: CpiContextPool.acquire (原子池分配)
                        -> SlotWriter.write (写入 slot)
                        -> ProgressTracker.advance (推进进度)
                        -> finalize_active (如触发):
                             -> CpiFinalizer.try_finalize → CpiOutput
                             -> output_ring.push(CpiOutput) [lock-free]
              -> CaptureSink.write (格式化输出)
         -> MetricsCollector.on_burst (指标收集)
    -> backend.release_burst

    -> CpiStateCoordinator.check_timeout (T-004 超时)
    -> CpiStateCoordinator.drain_recycle [从 recycle_ring 回收池位]
    -> RuntimeStatusReporter.emit_periodic (周期性状态报告)

收包循环结束:
  -> CaptureSink.flush
  -> CpiStateCoordinator.release_active (释放活动上下文)
  -> consumer_stop = true; consumer_thread.join()
  -> CpiStateCoordinator.drain_recycle (排空剩余回收令牌)
  -> RuntimeStatusReporter.build_final_summary
  -> RuntimeStatusReporter.render_final (渲染最终报告)
```

### CPI 输出流水线（Phase 5）

```
Producer (主线程 / OwnerLoop)          Consumer (后台线程 / CpiConsumer)
─────────────────────────────          ─────────────────────────────────

CpiStateCoordinator
  .finalize_active()
    ├─ CpiFinalizer.try_finalize()
    │   → 生成 CpiOutput
    └─ output_ring.push(CpiOutput)  ─→  output_ring.pop()
                                           ├─ handler(CpiOutput) [用户回调]
                                           └─ recycle_ring.push(ReleaseToken)
drain_recycle()  ←────────────────────
  ├─ recycle_ring.pop()
  └─ CpiContextPool.release(index)
```

**关键设计**：
- output_ring 和 recycle_ring 均为 SpscRing，lock-free 无锁通信
- CpiContextPool 使用 CAS 原子操作分配/释放 4 个 ~16 MB 的上下文 slot
- 池位在 push 到 output_ring 后保持占用，直到消费线程返回 ReleaseToken
- output_ring 满时 backpressure 策略：丢弃并立即释放池位

### 组件协作关系

```
OwnerLoop (协调器, ~155 行)
  ├─ PacketPipeline (协议处理)
  │   ├─ UdpPayloadAssembler
  │   ├─ PacketParser
  │   ├─ PacketValidator
  │   └─ ProtocolSequenceInterpreter
  ├─ CpiStateCoordinator (状态管理 + 输出流水线)
  │   ├─ CpiContextPool (原子化 4-slot 池)
  │   ├─ CpiAdmission + RecentClosedRing
  │   ├─ SlotWriter
  │   ├─ ProgressTracker
  │   ├─ CpiFinalizer
  │   ├─ SpscRing<CpiOutput> output_ring ──→ CpiConsumer (后台线程)
  │   └─ SpscRing<ReleaseToken> recycle_ring ←─┘
  ├─ DataOrderTracker (顺序验证)
  ├─ CaptureSink (输出格式化)
  └─ RuntimeStatusReporter (监控报告)
      ├─ StatusPanelWriter
      └─ MetricsCollector
```

## 6. DPDK ingress 当前职责

`src/receiver/ingress/dpdk/dpdk_backend.cpp` 当前负责：

- DPDK EAL 初始化
- 端口和 queue 配置
- mbuf pool 创建
- 批量收包
- 原始收包统计
- 按需应答 ARP 请求

它向上层交付统一 `PacketDesc`，协议处理不在 ingress 层完成。

## 7. UDP payload 组装层

`src/receiver/protocol/udp_payload_assembler.cpp` 负责：

- 解析以太网头、IPv4 头、UDP 头
- 提取 UDP payload
- 在需要时做 IPv4 分片重组
- 输出 `UdpPayloadFrame`

## 8. 当前协议模型

### 8.1 ProtocolSpec

`include/rxtech/protocol_spec.h` 当前集中定义协议形状：

- 控制表 magic：`0x55AAFF00`
- 数据包 magic：`0x55AAFF03`
- 尾包 tail：`0x55AAFF30`
- 头部长度：16 字节
- UDP 包长：2048 字节
- 数据负载长度：2032 字节
- 每 PRT 3 个通道
- 每通道 9 个包
- 前 8 包每包 508 个 IQ
- 最后 1 包 476 个 IQ，后续补零必须全为 0

### 8.2 Parser / Validator / Interpreter

当前协议相关实现分别位于：

- `src/receiver/protocol/sample_packet_parser.cpp`
- `src/receiver/protocol/sample_packet_validator.cpp`
- `src/receiver/protocol/protocol_sequence_interpreter.cpp`

这三层共同完成：

- 解析 16 字节协议头
- 校验 channel / packet_index / tail / payload 长度
- 按 `ProtocolSpec` 给出最终序列语义

## 9. CPI 热路径骨架

### 9.1 Storage

- `CpiContext`（~16 MB 定长连续缓冲区）
  - `CpiHotHeader`：64 字节对齐状态机（CpiState、trigger_bits、IQ 计数、时间戳）
  - `BoundWaveSnapshotLite`：波形绑定元数据
  - `prt_summary` 数组：逐 PRT 完成跟踪
  - `slot_valid_bytes`：逐 slot 有效性
  - `payload`：2048 字节步幅的 IQ 数据 slot
- `CpiContextPool`（4-slot 池，原子 CAS acquire/release）
- `SlotWriter`（写入 slot、检测重复、V-006 PRT 范围校验）
- `ProgressTracker`（推进 PRT/channel 完成标志、触发判定：T-003 FullReady / T-005 WaveEnd / TailObserved）

### 9.2 Admit

- `CpiAdmission`：根据当前包的 CPI ID 与活动上下文、RecentClosedRing 判定准入
  - `WRITE_ACTIVE`：属于活动 CPI
  - `TRIGGER_CPI_SWITCH`：下一个 CPI（CPI+1）
  - `LATE_TO_CLOSED`：属于已 finalize 的 CPI（丢弃）
  - `DROP`：无效或乱序

### 9.3 Finalize

- `CpiFinalizer::try_finalize(ctx, trigger)` 评估触发类型：
  - `TriggerFullReady` → `COMPLETE_OK`（所有 slot 已填满）
  - `TriggerCpiSwitch | TriggerWaveEnd` → `INCOMPLETE_BUT_COMMITTABLE` 或 `ABNORMAL_CUTOFF_COMMIT`
  - `TriggerTimeout | TriggerStop` → 返回 nullopt（静默丢弃）
- 输出 `CpiOutput`（cpi_id、decision、received_slot_count、timing、CpiReadOnlyView、pool_index）
- 上下文状态转移为 `SEALED`

### 9.4 Output Pipeline（Phase 5）

- `SpscRing<CpiOutput>` output_ring：主线程 push，消费线程 pop
- `CpiConsumer`：后台线程 busy-wait 弹出 CpiOutput，调用用户 handler 回调，推送 ReleaseToken 到 recycle_ring
- `SpscRing<ReleaseToken>` recycle_ring：消费线程 push，主线程 drain
- `ReleaseToken`（output_id + ctx_pool_index）：告知主线程可安全释放池位
- 主线程在每轮收包循环末尾调用 `drain_recycle()` 执行池位回收
- `CpiConsumer.processed_count_` 使用 `std::atomic<uint64_t>`，避免 `-O2` 下的数据竞争

## 10. Metrics 与总结

### 10.1 MetricsCollector 指标收集

当前 `MetricsCollector` 重点记录：

- `raw_rx_packets` / `raw_rx_bytes`
- `filtered_packets`
- `rx_packets`
- `parsed_packets`
- `control_table_packets`
- `data_packets`
- `dropped_packets`
- `captured_packets`
- `cpi_count` / `prt_count` / `channel_count`
- `active_cpi` / `active_prt` / `active_prt_channels`
- `complete_prt_count`
- `final_tail_packets`
- raw_record 相关统计

### 10.2 DataOrderTracker 顺序验证指标

新增的顺序追踪器提供：

- `checked_packets`：已检查的包数量
- `matches_expected`：是否匹配期望顺序
- `first_mismatch`：首次失配的位置描述
- `channel_batched_`：是否按通道批量到达

### 10.3 OwnerLoopRuntimeState 运行时状态

新增的运行时状态管理：

- `run_status` / `run_error`：运行状态和错误信息
- `protocol_packets` / `data_packets` / `captured_packets`：各阶段包计数
- `filtered_packets`：过滤掉的包数量
- CPI/PRT/Channel 活动状态追踪

### 10.4 RuntimeStatusReporter 状态报告

**周期性状态报告** (`emit_periodic`)：
- 按 `status_interval_seconds` 周期重绘中文状态面板
- 整合 Metrics、DataOrderTracker、OwnerLoopRuntimeState 生成 RunSummary
- 发送反馈快照（每 `feedback_interval_` 秒）

**最终总结生成** (`build_final_summary`)：
- 计算运行时长
- 汇总所有指标
- 填充人类可读的 summary 字符串
- 包含数据顺序验证结果

**渲染最终报告** (`render_final`)：
- 输出中文 `human_summary`
- 显示详细的统计表格
- 标记任何检测到的异常（如乱序）

### 10.5 状态面板显示逻辑

程序结束后会输出：

- 中文 `human_summary`
- 若 `run_until_stopped = true`，按 `status_interval_seconds` 重绘状态面板
- 未见业务协议流量时，状态面板只显示统一的链路层单面板
- 一旦见到业务协议流量，状态面板切换为协议层 + 结果层扩展视图
- 结果层中的 `channel_count` 是全局去重结果；若要判断某个当前 PRT 是否仍在接收中，应看 `active_prt_channels` 对应的逐通道覆盖
- **新增**：显示数据顺序验证状态（是否失配、首次失配位置）

## 11. 当前验证边界

- Windows 侧只用于读代码、改代码、改文档
- 权威构建、测试、运行验证必须在 Linux 服务器完成
- 当前测试规模：
  - **15 个 unit test 可执行文件**
  - **1 个 integration test 可执行文件**
  - 服务器验证通过：16/16 全部通过

### 测试覆盖矩阵

| 测试文件 | 覆盖模块 |
|---|---|
| test_spsc_ring | SpscRing push/pop、wrap-around、并发生产消费 |
| test_cpi_output_pipeline | **Phase 5 完整流水线**：pool acquire → finalize → output_ring → consumer → recycle → release |
| test_metrics | MetricsCollector 事件、clone、absorb、finalize |
| test_cli_args | CLI 参数解析 |
| test_rx_config | 配置文件加载 |
| test_merge_config | 配置合并逻辑 |
| test_sample_packet_parser | 从原始以太帧解析包 |
| test_sample_packet_validator | 语义校验（7 个场景） |
| test_protocol_sequence_interpreter | 序列解释、IQ 计数、补零规则 |
| test_slot_writer | slot 写入、重复检测、V-006、T-005 |
| test_cpi_admission | 准入判定（active/switch/late/drop） |
| test_udp_payload_assembler | UDP 重组（非分片 + 双分片） |
| test_owner_loop_summary | 人类可读总结、状态面板 |
| test_arp_responder | ARP 请求解析和应答构建 |
| test_runtime_legacy_xdp_config | 遗留 XDP 配置映射 |
| test_receive_runner_fake | 端到端 fake backend（正常流、不可用后端、run-until-stopped、源 IP 过滤） |

## 12. 架构演进总结

### 12.1 Phase 3：模块化重构

针对承担过多职责的臃肿主循环（超过 1000 行），采用按职责分层的策略进行组件拆分：

- 主循环代码量从 ~1000 行缩减至 ~155 行
- 形成 5 个独立组件：PacketPipeline、CpiStateCoordinator、DataOrderTracker、CaptureSink、RuntimeStatusReporter
- 实现单一职责、可测试性提升、模块可替换性

### 12.2 Phase 5：CPI 输出流水线

在 Phase 3 架构基础上，实现了从 CPI finalize 到消费再到池回收的完整异步闭环：

| 组件 | 实现内容 |
|---|---|
| SpscRing\<T\> | lock-free 环形缓冲区，原子操作 + 缓存行对齐 |
| CpiContextPool | 4-slot 原子池，CAS acquire/release |
| CpiConsumer | 后台消费线程，busy-wait 弹出 + handler 回调 |
| ReleaseToken | 回收令牌，消费线程 → 主线程池位释放 |

**Phase 5 关键约束与教训**：
- CpiContext ~16 MB，必须堆分配（栈分配导致服务器端栈溢出）
- SpscRing 容量语义：min_capacity 为下界，内部上取 power-of-two(min+1)-1 可用 slot
- CpiConsumer.processed_count_ 必须是 std::atomic 否则 -O2 下 busy-wait 变死循环

### 12.3 代码量对比

| 模块 | Phase 2 前 | Phase 3 后 | Phase 5 当前 |
|------|--------|--------|--------|
| owner_loop.cpp | ~1000 行 | ~120 行 | ~155 行（增加输出流水线创建） |
| 协议处理 | 分散在多处 | PacketPipeline 统一调度 | 不变 |
| 状态管理 | 嵌入主循环 | CpiStateCoordinator 独立 | 增加 output_ring/recycle_ring 管理 |
| CPI 输出 | 同步直接释放 | 同步直接释放 | 异步流水线：ring + consumer + recycle |
| 测试覆盖 | — | 14 unit + 1 integration | 15 unit + 1 integration (16/16) |

## 13. 结论

当前接收端的真实现状可以概括为：

> `rx_tech_demo` 当前已经形成一条以 DPDK 为唯一入口、以 UDP payload 组装和协议解析为核心、以 capture/raw_record 落盘和语义化统计为输出、已完成**模块化架构** + **CPI 异步输出流水线**的 Linux-only 接收端主线。

### 已落地能力

1. **模块化架构**（Phase 3）：主循环精简至 ~155 行，5 个职责清晰的独立模块
2. **完整协议解析链**：UdpPayloadAssembler → PacketParser → PacketValidator → ProtocolSequenceInterpreter
3. **CPI 生命周期管理**：准入(Admission) → 写入(SlotWriter) → 进度(ProgressTracker) → finalize(CpiFinalizer)
4. **异步输出流水线**（Phase 5）：lock-free SpscRing + 原子 CpiContextPool + CpiConsumer 后台线程
5. **运行时监控**：中文状态面板、数据顺序验证、最终总结报告
6. **原始帧录制**：后台写入线程、文件轮转、ringbuffer 缓冲

### 当前边界

- 它已经不是 AF_XDP benchmark 主线
- 还不是最终完整业务接收模块
- 外部发送端的真实闭环网络验证依赖服务器环境和发送工具就位
- 文档描述必须保持这两个边界
