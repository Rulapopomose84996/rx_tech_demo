1. 对象池想做什么？我们的接收逻辑中为什么需要她——CpiContextPool？使用它的逻辑是什么？我还是不太理解。我的粗浅理解就是 划定了一块地方用了专门呢做一件事情，我们定义指向这些地方的指针，然后使用指针操作这些地方，而不用频繁申请内存空间？这算不算空间换时间？

2. “write_to_file(out.data_ptr, out.size);  // ← 正在写入” 对于算法流水线来说，这是一次深拷贝吧，他可能是什么呢？ 还是说整个CPI在这个内存上完成CPU向GPU的拷贝之后，才释放，雷达信号处理算法中，在此之前（CPU向GPU拷贝）有没有需要拷贝的算法？

3. 把 `docs\2024-04-03_问题梳理与方案评审\问题与待办清单.md` 的学习笔记内容单独摘出来，写为新文档，并重新排版，按照人容易学习和理解的文档编写方式完成。

