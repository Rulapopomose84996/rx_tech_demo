# 平台与环境适配说明

## 1. 文档目的与适用范围

本文档用于说明 `rx_tech_demo` 在目标服务器平台上的适配边界、环境约束、依赖条件、硬件要求与运行前置条件，为后续在同类麒麟 V10 ARM64 平台上开展 `socket`、`AF_XDP`、`DPDK` 三类接收后端验证提供统一依据。

本文档适用于：

- 项目名称：高性能服务器接收端技术验证 Demo
- 代码仓库：`/home/devuser/WorkSpace/rx_tech_demo`
- 目标平台：Kylin Linux Advanced Server V10 (GFB), aarch64
- 目标后端：`socket + recvmmsg`、`AF_XDP`、`DPDK`
- 目标运行模式：`RX-only`、`RX + 轻量解析`、`RX + SPSC handoff`

本文档内容由两部分组成：

- 已探查确认的服务器事实
- 结合项目目标给出的适配建议和约束

截至 2026-03-17，服务器已完成基础探查，但 `/home/devuser/WorkSpace/rx_tech_demo` 当前仍是空目录，尚未完成正式 clone。

## 2. 当前目标平台概述

### 2.1 已探查确认的平台信息

通过 `ssh kds` 在服务器上探查到的环境如下：

- 主机名：`Radar`
- 操作系统：`Kylin Linux Advanced Server V10 (GFB)`
- 内核版本：`4.19.90-52.23.v2207.gfb08.ky10.aarch64`
- CPU 架构：`aarch64`
- CPU 厂商：`Phytium`
- 逻辑 CPU 数：`32`
- 每核线程数：`1`
- NUMA 节点数：`2`
- NUMA 节点 0 CPU：`0-15`
- NUMA 节点 1 CPU：`16-31`
- 内存总量：约 `62 GiB`
- Swap：`8 GiB`

### 2.2 平台适配结论

该平台满足本项目开展高性能接收技术验证的基础条件：

- ARM64 架构与项目目标平台一致
- GCC 和 CMake 版本符合麒麟 V10 旧环境约束
- 单路 32 物理核、双 NUMA 拓扑适合开展单阵面和三阵面接收对比
- 已具备多张多端口网卡资源，可用于不同后端的并行验证

但当前仍存在两个必须补齐的条件：

- 项目尚未在服务器上完成正式 clone
- `AF_XDP`/`DPDK` 所需用户态依赖当前未探查到系统级安装结果

## 3. 操作系统与基础软件版本要求

### 3.1 已确认版本

服务器实探结果如下：

- 操作系统：`Kylin Linux Advanced Server V10 (GFB)`
- 内核：`4.19.90-52.23.v2207.gfb08.ky10.aarch64`
- GCC：`7.3.0`
- G++：`7.3.0`
- CMake：`3.16.5`
- Clang：`18.1.8`
- Python：`3.7.9`
- `bpftool`：存在
- `libnuma`：存在

### 3.2 适配要求

本项目建议以以下版本基线为正式适配范围：

- `cmake_minimum_required(VERSION 3.16)`
- `C++17`
- 默认编译器使用 `GCC 7.3.0`
- 不依赖 GCC 8+/CMake 3.20+ 才具备的构建特性
- 核心路径避免直接依赖 `std::filesystem`

### 3.3 对 AF_XDP/DPDK 的额外说明

当前探查结果显示：

- `pkg-config --modversion libbpf` 未返回版本
- `pkg-config --modversion libxdp` 未返回版本
- `pkg-config --modversion libdpdk` 未返回版本
- `ldconfig -p` 中未发现 `libbpf`、`libxdp`、`dpdk` 相关条目

这说明截至探查时：

- 服务器默认系统路径中尚未确认具备 `AF_XDP` 用户态开发依赖
- 服务器默认系统路径中尚未确认具备 `DPDK` 开发依赖

因此，本项目第一阶段必须以 `socket` 基线后端作为可落地起点；`AF_XDP` 与 `DPDK` 进入开发前，需先补齐依赖提供方式。

## 4. 共享第三方依赖缓存适配要求

### 4.1 已确认的共享缓存目录

服务器上已存在共享第三方缓存根目录：

- `/home/devuser/WorkSpace/ThirdPartyCache`

已存在的缓存命名空间包括：

- `/home/devuser/WorkSpace/ThirdPartyCache/rma_manager`
- `/home/devuser/WorkSpace/ThirdPartyCache/qdgz300_backend`
- `/home/devuser/WorkSpace/ThirdPartyCache/ku2d-hmi`

其中已观察到的缓存内容包括：

- `googletest-1.14.0`
- `yaml-cpp-0.8.0`
- `spdlog-1.13.0`
- `nlohmann-json-3.11.3`
- `sqlite-amalgamation-3450000`
- `asio-1.30.2`
- `Crow-1.2.0`
- `boost_1_84_0`
- `CLI11-2.4.2`
- `tomlplusplus-3.4.0`

### 4.2 本项目适配要求

本项目禁止以“本地编译结果”为准，构建时应统一使用服务器共享缓存模式。

推荐为本项目建立独立缓存命名空间：

- `/home/devuser/WorkSpace/ThirdPartyCache/rx_tech_demo`

推荐结构：

```text
/home/devuser/WorkSpace/ThirdPartyCache/rx_tech_demo/
├── archives/
└── build/
    └── native-aarch64/
```

### 4.3 缓存使用原则

- 每个项目单独维护自己的缓存命名空间
- 依赖源码包放在 `archives/`
- 依赖中间构建产物放在 `build/native-aarch64/`
- 本项目优先使用离线包和共享缓存，不依赖服务器联网安装开发包
- 若 `AF_XDP`/`DPDK` 依赖无法完全纳入共享缓存，则需单独记录系统级安装步骤

## 5. 项目目录与远程工作区适配说明

### 5.1 目标工作区

本项目服务器目标目录固定为：

- `/home/devuser/WorkSpace/rx_tech_demo`

### 5.2 当前状态

实探结果表明：

- `/home/devuser/WorkSpace/rx_tech_demo` 目录已存在
- 当前目录为空
- 当前目录不是 git 仓库

因此，截至 2026-03-17，服务器侧还未完成正式拉取。

### 5.3 正式拉取要求

正式拉取必须满足以下条件：

1. 明确仓库远程地址
2. 在 `/home/devuser/WorkSpace` 下执行 clone
3. 若目录已存在且为空，可直接在其父目录重新 clone
4. 若目录后续已有内容，应使用 `git pull` 更新而不是覆盖

说明：

- 当前本地仓库尚未配置远程地址，因此服务器侧“拉取项目”暂时缺少来源信息
- 在未明确仓库来源前，不应伪造 clone 步骤

## 6. 硬件适配要求

### 6.1 CPU 与 NUMA

当前平台硬件满足以下要求：

- 32 个物理核
- 单核单线程，无 SMT 干扰
- 双 NUMA 节点
- NUMA 节点间距离 `22`，本地距离 `10`

适配建议：

- 接收线程、后处理线程和对应网卡尽量绑定在同一 NUMA 节点
- 单阵面测试至少保证 1 个专用接收核
- `RX + SPSC` 模式至少保证 1 个接收核和 1 个消费核
- 三阵面测试建议至少预留 6 个物理核，按阵面独占分配

### 6.2 内存

当前平台总内存约 `62 GiB`，满足本项目需要。

适配建议：

- `socket` 基线模式可在现有内存条件下直接开展
- `AF_XDP` 需额外为 UMEM 和 ring 预留连续内存预算
- `DPDK` 需额外为 hugepage 和 mempool 预留内存

### 6.3 hugepage 状态

当前探查到：

- `HugePages_Total: 0`
- `HugePages_Free: 0`
- `Hugepagesize: 524288 kB`

结论：

- 当前服务器未配置 hugepage
- `DPDK` 后端目前不具备直接运行前置条件

## 7. 网卡与驱动适配说明

### 7.1 已探查到的网卡资源

服务器当前可见两类主要网卡：

1. Intel X710 10GbE SFP+
2. Wangxun WX1860A4 Gigabit Ethernet Controller

对应 PCI 设备包括：

- `0001:05:00.0` ~ `0001:05:00.3`：Intel X710
- `0001:02:00.0` ~ `0001:02:00.3`：Wangxun WX1860A4

### 7.2 已探查到的接口状态

当前网络接口中与项目验证相关的主要口包括：

- `enP1s25f0`：UP
- `enP1s25f3`：UP
- `enP1s24f0`：UP

### 7.3 驱动与队列能力

实探结果如下：

#### Intel X710

- 接口：`enP1s25f0`、`enP1s25f3`
- 驱动：`i40e`
- 驱动版本：`2.25.9`
- firmware：`9.50 0x8000f12d 0.0.0`
- Combined queue 上限：`32`

#### Wangxun WX1860A4

- 接口：`enP1s24f0`
- 驱动：`ngbe`
- 驱动版本：`1.2.5`
- firmware：`0x00010019`
- Combined queue 上限：`8`

### 7.4 后端适配建议

#### socket 模式

- 两类网卡都可作为 `socket + recvmmsg` 基线验证对象
- 只要内核驱动正常、IP 配置可用、RSS/IRQ 可调，即可开展基线测试

#### AF_XDP 模式

- 是否支持 `XDP native` 和 `zero-copy` 仍需进一步逐卡验证
- 从队列数量和主流生态看，优先建议在 Intel X710 上开展 AF_XDP PoC
- `ngbe` 网卡是否具备稳定的 AF_XDP 能力需要单独验证，不应默认假定可用

说明：

- 上述“优先 Intel X710”的判断属于结合驱动生态和当前队列能力得出的工程推断，不是本次探查直接证明的结论

#### DPDK 模式

- 当前平台已加载 `vfio`、`vfio_iommu_type1` 模块
- IOMMU 已启用并能看到设备 group
- 因此平台具备开展 DPDK 前置验证的基础条件
- 但 hugepage 尚未配置，网卡尚未切换到 DPDK 驱动，故当前不具备直接运行条件

## 8. 网络拓扑与端口映射适配要求

本项目目标是比较不同接收后端在相同流量模型下的公平性，因此网络拓扑必须固定。

建议约束如下：

- 单阵面对应单物理口或单 RX queue
- 三阵面测试优先采用三物理口或三独立 queue 映射
- 所有对比测试必须固定 MTU、端口速率、流量模型、包长分布和发流源
- 需要在测试记录中明确“阵面 -> 网口 -> queue -> CPU -> NUMA”映射关系

推荐优先使用 Intel X710 的三个端口或三个 queue 承载三阵面对比场景，原因是：

- 队列资源更充足
- 10GbE 更符合高带宽验证目标
- 更利于后续扩展 AF_XDP / DPDK

## 9. NUMA 与 CPU 绑定要求

### 9.1 绑定原则

- 网卡所在 NUMA 节点优先匹配接收线程 CPU
- 后处理线程应尽量与对应接收线程位于同 NUMA 节点
- 不同阵面之间避免共享接收核
- 三阵面并发场景下，不同阵面不得复用同一个核心

### 9.2 推荐绑核策略

单阵面：

- 1 个接收核
- 1 个解析或消费核
- 所有相关线程位于网卡所在 NUMA 节点

三阵面：

- 每个阵面至少 1 个接收核
- 如使用 `SPSC` 模式，每个阵面额外 1 个消费核
- 共计建议 `6` 个物理核起步

### 9.3 IRQ / RSS 约束

- `socket` 模式必须记录 RSS 与 IRQ affinity 配置
- `AF_XDP` 模式必须明确 queue id 和 XDP redirect 关系
- `DPDK` 模式必须明确 lcore、port、queue 绑定关系

## 10. 内存与缓冲区配置要求

### 10.1 socket 模式

建议配置项：

- `SO_RCVBUF` 显式设置
- 批量接收深度与 `recvmmsg()` 批次大小统一管理
- `PacketPool` 和 SPSC ring 大小必须可配置

### 10.2 AF_XDP 模式

至少应显式配置：

- UMEM 总大小
- frame 大小
- fill ring 深度
- completion ring 深度
- RX/TX ring 深度
- 是否强制 zero-copy

### 10.3 DPDK 模式

至少应显式配置：

- hugepage 大小与页数
- mempool 个数
- mbuf 数量
- mbuf cache size
- RX descriptor / TX descriptor 数量

说明：

- 当前 hugepage 为 0，因此 DPDK 配置项虽然需要设计入配置文件，但在服务器准备完成前不可直接验收

## 11. 系统初始化与运行前置条件

### 11.1 socket 模式

前置条件：

- 网口处于 `UP`
- IP 已配置且可绑定
- `SO_RCVBUF` 与系统 `rmem` 限额匹配
- RSS/IRQ 策略已固定

### 11.2 AF_XDP 模式

前置条件：

- 内核支持 XDP/AF_XDP
- 驱动支持目标运行模式
- `bpftool` 或等价工具可用于查看 attach 状态
- libbpf 等用户态依赖已提供
- queue id 与网口绑定关系可控

当前状态：

- `bpftool` 已存在
- `libbpf` 尚未确认可用

### 11.3 DPDK 模式

前置条件：

- hugepage 已配置
- VFIO 可用
- IOMMU 已启用
- 网卡能切换到 DPDK 驱动
- 用户态依赖和 PMD 已可用

当前状态：

- `vfio` 已加载
- IOMMU 已启用
- hugepage 未配置
- DPDK 用户态库未确认存在

## 12. 兼容性矩阵

### 12.1 当前已确认矩阵

| 项目 | 当前状态 | 说明 |
| --- | --- | --- |
| OS | 已确认 | Kylin Linux Advanced Server V10 (GFB) |
| Kernel | 已确认 | 4.19.90-52.23.v2207.gfb08.ky10.aarch64 |
| GCC | 已确认 | 7.3.0 |
| CMake | 已确认 | 3.16.5 |
| NUMA | 已确认 | 2 节点 |
| Intel X710 + socket | 可开展 | 内核驱动正常，队列能力充足 |
| Wangxun WX1860A4 + socket | 可开展 | 可作为基线补充验证对象 |
| Intel X710 + AF_XDP | 待验证 | 需确认 libbpf 和驱动实际支持情况 |
| Wangxun WX1860A4 + AF_XDP | 高风险待验证 | 不建议直接作为首选验证对象 |
| Intel X710 + DPDK | 待补齐前置条件 | hugepage 与用户态依赖缺失 |
| Wangxun WX1860A4 + DPDK | 待验证 | 驱动和 PMD 路径需额外确认 |

### 12.2 当前最稳妥的技术路线

按当前环境成熟度排序：

1. `socket + recvmmsg`
2. `AF_XDP` on Intel X710
3. `DPDK` on Intel X710

## 13. 性能测试环境约束

为保证结果可信，测试期间必须满足以下约束：

- 关闭调试日志和额外抓包
- 固定 CPU 频率，避免 governor 抖动影响
- 测试期间避免其他业务抢占目标核心
- 固定网卡速率、MTU、流量模型、包型和时长
- 固定 RSS、IRQ affinity、NUMA、CPU 绑核
- 同一组比较必须复用相同的后处理链和指标采集逻辑

不建议将虚拟机环境、容器网络虚拟化环境结果作为正式结论。

## 14. 部署方式与运行模式适配说明

### 14.1 单阵面

建议部署方式：

- 1 个物理口或 1 个 queue
- 1 个接收线程
- 如使用 `SPSC`，再加 1 个消费线程

### 14.2 三阵面

建议部署方式：

- 3 个独立端口，或 1 张多队列网卡的 3 个独立 queue
- 每阵面独占接收线程
- `SPSC` 模式下每阵面独占消费线程

### 14.3 结果输出目录

项目运行结果建议统一输出到：

- 项目内相对目录：`results/`
- 服务器正式运行目录：可映射至 `/home/devuser/WorkSpace/rx_tech_demo/results`

## 15. 验证与自检方法

建议将以下探查命令固化到后续自检脚本中：

- 查看系统版本：`uname -a`、`cat /etc/os-release`
- 查看编译器：`gcc --version`、`cmake --version`
- 查看 NUMA：`numactl --hardware`
- 查看 CPU：`lscpu`
- 查看内存与 hugepage：`free -h`、`grep -i huge /proc/meminfo`
- 查看网卡：`ip -br link`
- 查看驱动与队列：`ethtool -i <nic>`、`ethtool -l <nic>`
- 查看 PCI 网卡设备：`lspci | grep -i -E 'ethernet|network'`
- 查看 VFIO：`lsmod | grep vfio`
- 查看 IOMMU：`dmesg | grep -i iommu`
- 查看 AF_XDP 基础能力：`bpftool version`

## 16. 已知问题、限制与风险

### 16.1 当前已确认问题

- `/home/devuser/WorkSpace/rx_tech_demo` 当前为空目录，不是 git 仓库
- 当前本地仓库未配置远程地址，因此服务器无法完成正式 clone
- 服务器默认路径中未确认 `libbpf`、`libxdp`、`DPDK` 用户态开发依赖
- 服务器尚未配置 hugepage

### 16.2 风险说明

- 若 AF_XDP 驱动仅支持 copy mode，则收益可能有限
- 若 DPDK 驱动绑定需要改动现网网卡归属，则需严格隔离验证窗口
- 若 NUMA、IRQ、queue、绑核不固定，对比结果会失真
- 若直接使用 WX1860A4 作为 AF_XDP/DPDK 首批验证对象，风险高于 Intel X710

## 17. 回退方案

当高性能后端条件不满足时，建议按以下顺序回退：

1. DPDK 不满足前置条件时，回退到 AF_XDP
2. AF_XDP 不满足前置条件时，回退到 `socket + recvmmsg`
3. 若共享缓存依赖未准备完成，优先保证 `socket` 基线能在服务器原生构建和测试

这也是本项目当前最合理的实施顺序。

## 18. 结论

基于 2026-03-17 的服务器实探结果，可以得出如下结论：

1. 当前麒麟 V10 ARM64 平台适合作为本项目正式适配平台
2. `socket + recvmmsg` 已具备最先落地条件，应作为第一阶段基线
3. Intel X710 更适合作为 AF_XDP / DPDK 的优先验证网卡
4. AF_XDP 与 DPDK 的用户态依赖、hugepage、驱动绑定和共享缓存命名空间仍需补齐
5. 服务器工作区中的 `rx_tech_demo` 尚未完成正式 clone，后续必须在明确仓库远程地址后补齐

在上述条件补齐前，本项目的正式工程结论应基于：

- 服务器原生环境
- 共享第三方缓存
- 统一场景和统一指标
- 先 `socket`、后 `AF_XDP`、最后 `DPDK` 的推进顺序
