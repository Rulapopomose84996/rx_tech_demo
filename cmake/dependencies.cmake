set(RXTECH_THIRD_PARTY_CACHE "${RXTECH_THIRD_PARTY_CACHE}" CACHE PATH "Shared third-party cache root used by server build helpers")
mark_as_advanced(RXTECH_THIRD_PARTY_CACHE)

# Placeholder for future third-party dependency resolution.
# Keep the first revision self-contained so the project can configure
# on Windows, WSL and old Linux environments without extra packages.
